import PerformanceComingSoon from './PerformanceComingSoon';

const SelfMapKRAs = () => {
    return (
        <PerformanceComingSoon
            title="Self-Map KRAs"
            desc="Add, propose, or link new Key Result Areas (KRAs) that relate directly to your custom tasks and specialized roles."
            plannedFeatures={[
                'Custom KRA proposals',
                'Manager approval request flow',
                'Competency linking interface',
                'Historical proposal logs'
            ]}
        />
    );
};

export default SelfMapKRAs;
