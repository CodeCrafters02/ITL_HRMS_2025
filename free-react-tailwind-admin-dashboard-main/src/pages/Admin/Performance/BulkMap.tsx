import AdminPerformanceComingSoon from './AdminPerformanceComingSoon';

const BulkMap = () => {
    return (
        <AdminPerformanceComingSoon
            title="Bulk Map KRAs & Competencies"
            desc="Map Key Result Areas, expected skill competencies, and standard objectives in bulk to multiple designations or departments at once."
            plannedFeatures={[
                'Department-wide mapping panels',
                'Designation assignment check matrices',
                'Multi-role template mappings overrides',
                'Bulk deployment execution logs'
            ]}
        />
    );
};

export default BulkMap;
