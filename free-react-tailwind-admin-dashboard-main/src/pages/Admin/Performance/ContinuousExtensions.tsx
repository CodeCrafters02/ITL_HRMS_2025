import AdminPerformanceComingSoon from './AdminPerformanceComingSoon';

const ContinuousExtensions = () => {
    return (
        <AdminPerformanceComingSoon
            title="Continuous Review Extensions"
            desc="Manage grace extensions and timeline overrides specifically for missed continuous check-in and goals update deadlines."
            plannedFeatures={[
                'Ongoing check-in delay flags audits',
                'Bulk extension triggers overrides',
                'Manager delegation logs tracker',
                'Alert systems configuration panel'
            ]}
        />
    );
};

export default ContinuousExtensions;
